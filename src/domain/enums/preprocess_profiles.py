from enum import Enum


class PreprocessProfile(Enum):
    """Профили предобработки изображения для детектора изображений."""

    LIGHT_CATALOG = "light"
    """Светлый/белый фон — типичный печатный каталог."""

    DARK_CATALOG = "dark"
    """Тёмный фон — инвертированный или ночной стиль."""

    LOW_CONTRAST = "low"
    """Выцветший скан с низким контрастом."""

    DENSE_GRID = "dense"
    """Много мелких карточек товаров на одной странице."""
